Print
    ksnip

super + s
    screenshot selection

super + shift + s
    screenshot last-selection

super + shift + a
    screenshot

super + l
    slock

super {_, shift + } d
    {date_format, datetime_format} | head -c -1 | gpaste-client

ctrl + shift + v
    gpaste-client ui

ctrl + shift + v + Left
    gpaste-client --use-index select 1
