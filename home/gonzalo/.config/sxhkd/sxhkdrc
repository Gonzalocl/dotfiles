Print
    ksnip

super + s
    ksnip -r

super + shift + s
    screenshot last-selection

super + shift + a
    screenshot

super + l
    slock

super + d
    date_format | xclip -i -r -selection clipboard && xte "keyup d" "keydown Control_L" "key v" "keyup Control_L"

super + shift + d
    datetime_format | xclip -i -r -selection clipboard && xte "keyup d" "keydown Control_L" "key v" "keyup Control_L"
