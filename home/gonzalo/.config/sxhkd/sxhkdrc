Print
    ksnip

super + a
    echo

super + {_, shift + } {s, a}
    screenshot {selection, } {last-selection, }

super + l
    slock

super + {_, shift + } d
    {date_format, datetime_format} | head -c -1 | gpaste-client

ctrl + shift + v
    gpaste-client ui
