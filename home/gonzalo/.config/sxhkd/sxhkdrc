
Print
    ksnip

super + s
    ksnip -r

super + shift + s
    ksnip -ls

super + shift + a
    ksnip -fs

super + l
    slock

super + d
    xte "keyup Super_L" ; sleep 0.1 ; xte "str "`date_format` ; xte "keydown Super_L"
