Print
    ksnip

super + s
    screenshot selection

super + shift + s
    screenshot last-selection

super + shift + a
    screenshot

super + l
    slock

super + d
    date_format | head -c -1 | gpaste-client

super + shift + d
    datetime_format | head -c -1 | gpaste-client

super + v
    gpaste-client ui